<Window x:Class="MovieApp.MVVM.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:MovieApp.MVVM.ViewModel"
        Title="Bejelentkezés / Regisztráció" 
        Height="250" 
        Width="350"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0 0 0 10">
            <TextBlock Text="Felhasználónév:"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0 0 0 10">
            <TextBlock Text="Jelszó:"/>
            <PasswordBox x:Name="PasswordBox" Width="200" PasswordChanged="PasswordBox_PasswordChanged"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 10 0 0">
            <Button Content="Bejelentkezés" 
                    Margin="5" 
                    Command="{Binding LoginCommand}" 
                    Width="100"/>
            
            <Button Content="Regisztráció" 
                    Margin="5" 
                    Command="{Binding RegisterCommand}" 
                    Width="100"/>
        </StackPanel>
    </Grid>
</Window>