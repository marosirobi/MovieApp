<UserControl x:Class="MovieApp.MVVM.View.ReviewsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MovieApp.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#272537">

    <Grid Margin="30">
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Width="400">
            <!-- Eddig írt vélemények -->
            <TextBlock Text="Your Reviews" FontSize="32" Foreground="White" Margin="0,0,0,20"/>

            <ListBox x:Name="ReviewsListBox" Height="150" Margin="0,0,0,20">
                <!-- Itt jelennek meg az eddigi vélemények -->
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Title}" Foreground="White" FontSize="18"/>
                            <TextBlock Text="{Binding Review}" Foreground="White" FontSize="14" TextWrapping="Wrap"/>
                            <TextBlock Text="Rating: {Binding Rating}" Foreground="White" FontSize="14"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Új vélemény írás gomb -->
            <Button Content="Write New Review" Width="150" Click="WriteNewReview_Click" HorizontalAlignment="Left" Margin="0,0,0,20"/>

            <!-- Új vélemény űrlap -->
            <StackPanel x:Name="NewReviewPanel" Visibility="Collapsed">
                <TextBlock Text="Title:" Foreground="White" FontSize="16" Margin="0,0,0,5"/>
                <TextBox x:Name="TitleTextBox" Height="30" Margin="0,0,0,15"/>

                <TextBlock Text="Review:" Foreground="White" FontSize="16" Margin="0,0,0,5"/>
                <TextBox x:Name="ReviewTextBox" Height="100" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,15"/>

                <TextBlock Text="Rating (1-10):" Foreground="White" FontSize="16" Margin="0,0,0,5"/>
                <ComboBox x:Name="RatingComboBox" Height="30" Margin="0,0,0,15">
                    <ComboBoxItem Content="1"/>
                    <ComboBoxItem Content="2"/>
                    <ComboBoxItem Content="3"/>
                    <ComboBoxItem Content="4"/>
                    <ComboBoxItem Content="5"/>
                    <ComboBoxItem Content="6"/>
                    <ComboBoxItem Content="7"/>
                    <ComboBoxItem Content="8"/>
                    <ComboBoxItem Content="9"/>
                    <ComboBoxItem Content="10"/>
                </ComboBox>

                <Button Content="Save Review" Width="100" Click="SaveReview_Click" HorizontalAlignment="Left" Margin="0,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
