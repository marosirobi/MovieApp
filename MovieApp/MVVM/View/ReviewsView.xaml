<UserControl x:Class="MovieApp.MVVM.View.ReviewsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MovieApp.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#272537">

    <Grid Margin="30">
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Width="400">
            <TextBlock Text="Your Reviews" FontSize="32" Foreground="White" Margin="0,0,0,20"/>

            <!-- Értékelt filmek listája -->
            <ListBox x:Name="ReviewsListBox" Height="150" Margin="0,0,0,20">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Title}" Foreground="White" FontSize="18"/>
                            <TextBlock Text="Rating: {Binding Rating}/10" Foreground="White" FontSize="14"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Film kiválasztása RapidAPI-ról -->
            <TextBlock Text="Select a movie:" Foreground="White" FontSize="16" Margin="0,0,0,5"/>
            <ComboBox x:Name="FilmComboBox" Height="30" Margin="0,0,0,15"/>

            <!-- Értékelés választása -->
            <TextBlock Text="Rating (1-10):" Foreground="White" FontSize="16" Margin="0,0,0,5"/>
            <Slider x:Name="RatingSlider" Minimum="1" Maximum="10" Value="5" Width="200" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,0,0,10"/>
            <TextBlock x:Name="RatingValueText" Text="5" Foreground="White" Margin="0,0,0,15"/>

            <!-- Mentés gomb -->
            <Button Content="Save Review" Width="100" Click="SaveReview_Click" HorizontalAlignment="Left"/>
        </StackPanel>
    </Grid>
</UserControl>
